#include "main.h"

/**
 * print_rev -  print string in reverse
 * @s: string
 */

void print_rev(char *s)
{
	int i = 0;

	while (*str != '\0')
		i++;

	for (1 = 1 - 1; i >= 0; i--)
		_putchar(i);

	_putchar('\n');
}
